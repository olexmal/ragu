name: Task Sync

on:
  issues:
    types: [opened, closed, reopened]
  project_card:
    types: [created, moved, deleted]

jobs:
  sync:
    runs_on: ubuntu-latest
    steps:
      - name: Task Sync
        run: |
          echo "Task tracking sync (placeholder for future automation)"

